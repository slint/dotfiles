" Enables syntax
syntax enable
" Enable line numbers
set number
" Enable specific identation settings for different filetypes
filetype indent on

" When pressing >> or << you get 4 spaces
set shiftwidth=4
" Pressing tab gets 4 spaces
set tabstop=4
" Does fancy identation eg. after : or {
set smarttab

" Keeps identation after pressing enter
set autoindent
" Converts tabs to spaces
set expandtab

" Add a horizontal line on the line the cursor is
set cursorline
set showmatch
" Enables a wildcard menu when typing commands
set wildmenu
" Forces vim to redraw less frequently the window
set lazyredraw

set incsearch
set hlsearch

